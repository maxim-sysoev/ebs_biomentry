name: ebs_biometry
description: A new flutter plugin project.
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

  surf_lint_rules: 0.0.2-dev.2

flutter:
  plugin:
    platforms:
      android:
        package: ru.surfstudio.ebs_biometry
        pluginClass: EbsBiometryPlugin
      ios:
        pluginClass: EbsBiometryPlugin
